spring:
  application:
    name: reservation-api
  datasource:
    url: jdbc:h2:mem:fabryka-terapii;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
  h2:
    console:
      enabled: true
server:
  error:
    include-message: always
